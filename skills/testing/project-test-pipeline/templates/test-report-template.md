# 测试报告 <项目名> <YYYY-MM-DD>（第 N 轮）

> 模板说明：由 project-test-pipeline 步骤 6 使用。铁律：一切结论必须有证据链，禁止「可能/也许/应该/大概/推测」；无法判定的标 UNCONFIRMED 并写明缺什么证据。**定稿前 grep 自查** `可能|也许|应该|大概|推测|猜测`，命中须为「待核实/未确认」语境，否则改写。

## 1. 基础信息

- 环境指纹：`<运行 scripts/collect-env-fingerprint.sh 的输出>`
  - OS / JDK / Maven / Node / Python / Docker 镜像 / 中间件版本 / swap / profile
- 被测 commit SHA：`<sha>`　基线 SHA：`<sha>`
- 触发人 / 执行起止时间：`<...>`
- 本次范围：全量回归 / 最小变更驱动回归集（加速模式必须附「风险范围：xxx」）

## 2. 分层汇总

| 层 | 总数 | PASS | FAIL | SKIP | LIMITED | 通过率 | 耗时 |
|---|---|---|---|---|---|---|---|
| L0 单元 |  |  |  |  |  |  |  |
| L1 Agent |  |  |  |  |  |  |  |
| L2 前端组件 |  |  |  |  |  |  |  |
| L3 接口 |  |  |  |  |  |  |  |
| L4 E2E 视觉 |  |  |  |  |  |  |  |
| **合计** |  |  |  |  |  |  |  |

通过率 = PASS / (PASS + FAIL)；FLAKY 独立统计不混入。

## 3. 缺陷清单（按 P0/P1/P2/P3 分级）

### P0 阻断（必须修复，不允许放行）
| 用例 ID | 层 | 现象 | 根因（证据链） | 日志/截图证据路径 | 状态 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

### P1 严重（优先修复，可协商降级开关）
| 用例 ID | 层 | 现象 | 根因（证据链） | 日志/截图证据路径 | 状态 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

### P2 一般 / P3 低优
- `用例 ID`：描述（登记技术债 / backlog）

> 失败明细必写：失败原因 + 日志片段 + 复现步骤，不能只写「失败」。

## 4. Flaky / 受限 / 未覆盖

- Flaky 清单：`<用例 ID + 触发条件 + 重试策略>`（同步维护 docs/test/flaky-cases.md）
- 受限明细（含补测条件）：`<用例 ID + LIMITED 原因 + 何时可补测>`
- 未覆盖模块风险清单：`<模块 + 未覆盖原因 + 风险等级>`

## 5. 结论

- 变更影响摘要：`<git diff <baseline> HEAD 的文件范围映射>`
- 上线建议：**放行 / 受限放行 / 阻塞**（受限与阻塞须列出条件）
- 回滚触发条件（上线告警阈值）：`<如：P95 超 2s 或错误率 >1% 触发回滚>`
- 缺陷交接：`<缺陷清单已交接用户，修复由用户/外部流程处理，验证在下一轮测试>`
- 边界声明：本流水线负责预合并门禁；生产灰度冒烟属于后置补充；测试通过 ≠ 自动上线，合并/上线决策权保留给用户。
